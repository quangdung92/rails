<style>
	.small-3.columns {
		height: 24px;
		border-right: 1px solid #8e8e8e;
		padding: 0px;
	}
	.row {
		border: 2px solid white;
		padding-left: 5px
	}
	h6 {
		font-size: 0.8em
	}
	h6 {
		color: white;
	}
	body {
		background-color: #7f7f7f;
	};
</style>
<div class="fixed" align="center">
	<nav class="top-bar" style="height: 25px">
		<div class="small-3 columns" >
			<h6>
				<i class="step fi-address-book size-12" style="color: white"></i>
				<%= current_user.name%></h6>
		</div>
		<div class="small-3 columns" >
			<h6><i class="step fi-home size-15" style="color: white"></i>
				<%= current_user.shop_no%></h6>
		</div>
		<div class="small-3 columns">
			<h6><i class="step fi-torso size-12" style="color: white"></i>
				<%= current_user.user_id.from(-4)%></h6>
		</div>
		<div class="small-3 columns" style="border-right: 0px">
			
			<h6><i class="step fi-calendar size-15" style="color: white"></i>
				<%= current_user.created_at.strftime("%y/%m/%d")%></h6>
		</div>
	</nav>
	<nav class="top-bar" style="background-color: #646478; height: 40px">
		<a href="save" class="small-4 columns" > <h5><img src="/assets/navicon.png" width="50" height="55" align="left"></h5> </a>
	</nav>
</div>
<br />
<br />
<br />
<div class="row" >
	<p>
		<%= link_to "All",
		{:controller => "welcome", :action => "product"},
		{:style => "color: white"}%>
	</p>
</div>
<%if @locations%>
<% @locations.each do |item| %>
<div class="row" >
	<p>
		<%= link_to item.location_name,
		{:controller => "welcome", :action => "product", :link_to => item.location_name},
		{:style => "color: white" }%>
	</p>
</div>
<%end%>
<%else if @genres%>
<% @genres.each do |item| %>
<div class="row" >
	<p>
		<%= link_to item.genre_name,
		{:controller => "welcome", :action => "product", :link_to => item.genre_name},
		{:style => "color: white" } %>
	</p>
</div>
<%end%>
<%else%>
<% @publishers.each do |item| %>
<div class="row" >
	<p>
		<%= link_to item.publisher_name,
		{:controller => "welcome", :action => "product", :link_to => item.publisher_name},
		{:style => "color: white" } %>
	</p>
</div>
<%end%>
<%end%>
<%end%>
